<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingTop="10dp"
    android:paddingBottom="10dp">

    <ImageView
        android:id="@+id/ranklist_image"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:src="@drawable/ic_launcher" />

    <TextView
        android:id="@+id/ranklist_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="16sp"
        android:textColor="@color/text_gray"
        android:layout_toRightOf="@+id/ranklist_image"
        android:layout_marginLeft="10dp"
        android:layout_marginTop="3dp"
        android:layout_alignTop="@+id/ranklist_image"
        android:text="@string/app_list_text"/>

    <com.catchgift.trashclear.view.MonitorProgressBar
        android:id="@+id/battery_ranklist_progress"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        style="@android:style/Widget.ProgressBar.Horizontal"
        android:layout_toRightOf="@+id/ranklist_image"
        android:layout_below="@+id/ranklist_name"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="10dp"
        android:layout_marginTop="3dp"
        android:max="100"/>

</RelativeLayout>
